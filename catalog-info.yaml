apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: dynamic-cluster-gazin-location
  description: Location to import all dynamic-cluster-gazin catalog components
spec:
  targets:
    - ./.platform/*.yaml
